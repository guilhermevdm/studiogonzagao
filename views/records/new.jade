extends ./_form

block prepend form-for
	form(action="add", method="post")
		.input-group
			input.form-control(type='text', name="record.name", placeholder=__("records.name"), required)
			span.input-group-btn
				button.btn.btn-success(type='submit') #{__("system.add")}